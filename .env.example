# Environment Configuration for mmWave Fall Detection

# Machine Learning Model Configuration
# Path to the trained FallNet model file
# Can be an absolute path or relative to the project root
# Default: ml/fallnet_lstm.pt
MODEL_PATH=ml/fallnet_lstm.pt

# API Server Configuration
# Host address for the FastAPI server
# 0.0.0.0 allows connections from any interface
# 127.0.0.1 restricts to localhost only
# Default: 0.0.0.0
API_HOST=0.0.0.0

# API Server port number
# Must be an available port on the system
# Common ports: 8000 (development), 5000, 3000
# Default: 8000
API_PORT=8000

# Logging Configuration
# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# DEBUG: Verbose output for troubleshooting
# INFO: Standard informational messages
# WARNING: Warning messages and above
# ERROR: Error messages only
# Default: INFO
LOG_LEVEL=INFO

# Isaac Sim / Omniverse Configuration
# Path to the hall USD stage file used in simulation (赤土崎多功能館)
# Only required when running simulation with record_fall_data.py
# Can be an absolute path or relative to the project root
# Example: /Isaac/Environments/Simple_Room/simple_room.usd
# Example: sim/assets/stages/hall.usd
# Optional: Leave empty or comment out if not using simulation
SIM_STAGE_PATH=/Isaac/Environments/Simple_Room/simple_room.usd

# GPU Configuration
# Comma-separated list of GPU device IDs to use
# Example: 0 (use first GPU)
# Example: 0,1 (use first and second GPUs)
# Leave empty to use all available GPUs
# Optional: Leave empty for automatic selection
CUDA_VISIBLE_DEVICES=
